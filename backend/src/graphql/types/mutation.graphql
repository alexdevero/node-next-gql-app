type AuthPayload {
  # token: String # todo: add token
  user: User!
}

type Mutation {
  login(email: String!, password: String!): AuthPayload!
  logout: Boolean
  signUp(username: String!, email: String!, password: String!): AuthPayload!
}
